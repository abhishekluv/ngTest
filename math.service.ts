export class MathService { // Define a simple service class
  constructor() {}

  add(num1: number, num2: number): number { // Method to add two numbers
    return num1 + num2; // Return the sum
  }
}
